import ModulePlaceholder from '../components/ModulePlaceholder'

export default function Tracks() {
  return (
    <ModulePlaceholder
      phase={2}
      title="Track Vault"
      tagline="Your catalogue and each track's status — idea → demo-ready → submitted → signed."
    />
  )
}
