import ModulePlaceholder from '../components/ModulePlaceholder'

export default function Contacts() {
  return (
    <ModulePlaceholder
      phase={3}
      title="Contacts CRM"
      tagline="Every label and industry contact in one place — the backbone the outreach layer reads from."
    />
  )
}
