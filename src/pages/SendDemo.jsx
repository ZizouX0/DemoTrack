import ModulePlaceholder from '../components/ModulePlaceholder'

export default function SendDemo() {
  return (
    <ModulePlaceholder
      phase={4}
      title="Send Demo"
      tagline="The friction-killer: pick a track, pick a label, send in under 3 minutes — every send logged."
    />
  )
}
